<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paiement confirmé - Error Explorer</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            padding: 0;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 300;
        }
        .content {
            padding: 30px;
        }
        .success-icon {
            text-align: center;
            margin-bottom: 20px;
        }
        .success-icon .icon {
            display: inline-block;
            width: 60px;
            height: 60px;
            background-color: #28a745;
            border-radius: 50%;
            line-height: 60px;
            color: white;
            font-size: 24px;
        }
        .plan-details {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .plan-details h3 {
            margin-top: 0;
            color: #495057;
        }
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }
        .detail-row:last-child {
            margin-bottom: 0;
            font-weight: bold;
            border-top: 1px solid #dee2e6;
            padding-top: 10px;
            margin-top: 10px;
        }
        .button {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            padding: 12px 30px;
            border-radius: 5px;
            font-weight: bold;
            text-align: center;
        }
        .features {
            margin: 30px 0;
        }
        .features ul {
            list-style: none;
            padding: 0;
        }
        .features li {
            padding: 5px 0;
            position: relative;
            padding-left: 25px;
        }
        .features li:before {
            content: "✓";
            position: absolute;
            left: 0;
            color: #28a745;
            font-weight: bold;
        }
        .footer {
            background-color: #f8f9fa;
            padding: 20px;
            text-align: center;
            font-size: 14px;
            color: #6c757d;
        }
        .footer a {
            color: #667eea;
            text-decoration: none;
        }
        @media (max-width: 600px) {
            .content {
                padding: 20px 15px;
            }
            .detail-row {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Error Explorer</h1>
        </div>
        
        <div class="content">
            <div class="success-icon">
                <div class="icon">✓</div>
            </div>
            
            <h2 style="text-align: center; color: #28a745; margin-bottom: 10px;">Paiement confirmé !</h2>
            <p style="text-align: center; font-size: 18px; color: #6c757d; margin-bottom: 30px;">
                Merci {{ user.firstName }}, votre abonnement est maintenant actif.
            </p>
            
            <div class="plan-details">
                <h3>Détails de votre abonnement</h3>
                <div class="detail-row">
                    <span>Plan :</span>
                    <span><strong>{{ subscription.plan.name }}</strong></span>
                </div>
                <div class="detail-row">
                    <span>Facturation :</span>
                    <span>{{ subscription.isYearly ? 'Annuelle' : 'Mensuelle' }}</span>
                </div>
                <div class="detail-row">
                    <span>Montant :</span>
                    <span>{{ subscription.formattedAmount }}</span>
                </div>
                <div class="detail-row">
                    <span>Période actuelle :</span>
                    <span>{{ subscription.currentPeriodStart | date('d/m/Y') }} - {{ subscription.currentPeriodEnd | date('d/m/Y') }}</span>
                </div>
                {% if subscription.isOnTrial %}
                <div class="detail-row">
                    <span>Essai gratuit jusqu'au :</span>
                    <span>{{ subscription.trialEnd | date('d/m/Y') }}</span>
                </div>
                {% endif %}
                <div class="detail-row">
                    <span>Prochaine facturation :</span>
                    <span>{{ subscription.currentPeriodEnd | date('d/m/Y') }}</span>
                </div>
            </div>
            
            {% if subscription.isOnTrial %}
            <div style="background-color: #d4edda; border: 1px solid #c3e6cb; border-radius: 5px; padding: 15px; margin: 20px 0;">
                <strong style="color: #155724;">🎉 Période d'essai activée !</strong>
                <p style="margin: 5px 0 0 0; color: #155724;">
                    Vous bénéficiez de {{ subscription.trialDaysRemaining }} jours d'essai gratuit. 
                    Votre premier paiement aura lieu le {{ subscription.trialEnd | date('d/m/Y') }}.
                </p>
            </div>
            {% endif %}
            
            <div style="text-align: center; margin: 30px 0;">
                <a href="{{ app.request.schemeAndHttpHost }}{{ path('dashboard_index') }}" class="button">
                    Accéder à mon dashboard
                </a>
            </div>
            
            <div class="features">
                <h3>Vous pouvez maintenant :</h3>
                <ul>
                    <li>Créer {{ subscription.plan.maxProjects == -1 ? 'un nombre illimité de' : 'jusqu\'à ' ~ subscription.plan.maxProjects }} projets</li>
                    <li>Recevoir {{ subscription.plan.maxMonthlyErrors == -1 ? 'un nombre illimité d\'' : 'jusqu\'à ' ~ subscription.plan.maxMonthlyErrors }} erreurs par mois</li>
                    <li>Conserver vos données pendant {{ subscription.plan.dataRetentionDays }} jours</li>
                    {% if subscription.plan.emailAlerts %}
                    <li>Configurer des alertes par email</li>
                    {% endif %}
                    {% if subscription.plan.slackIntegration %}
                    <li>Intégrer avec Slack et Discord</li>
                    {% endif %}
                    {% if subscription.plan.apiAccess %}
                    <li>Utiliser l'API complète</li>
                    {% endif %}
                    <li>Bénéficier du support technique prioritaire</li>
                </ul>
            </div>
            
            <div style="background-color: #f8f9fa; border-radius: 5px; padding: 20px; margin: 20px 0;">
                <h4 style="margin-top: 0;">Prochaines étapes</h4>
                <ol style="margin: 0; padding-left: 20px;">
                    <li>Créez votre premier projet dans le dashboard</li>
                    <li>Intégrez notre SDK dans votre application</li>
                    <li>Configurez vos alertes personnalisées</li>
                    <li>Explorez les analytics de vos erreurs</li>
                </ol>
            </div>
            
            <div style="text-align: center; margin: 30px 0; font-size: 14px; color: #6c757d;">
                <p>Besoin d'aide ? Notre équipe support est là pour vous accompagner.</p>
                <a href="{{ app.request.schemeAndHttpHost }}{{ path('home_contact') }}" style="color: #667eea;">
                    Contactez le support
                </a>
            </div>
        </div>
        
        <div class="footer">
            <p>
                <strong>Error Explorer</strong><br>
                Surveillance d'erreurs pour développeurs
            </p>
            <p>
                <a href="{{ app.request.schemeAndHttpHost }}{{ path('billing_dashboard') }}">Gérer mon abonnement</a> | 
                <a href="{{ app.request.schemeAndHttpHost }}{{ path('home_contact') }}">Support</a> | 
                <a href="{{ app.request.schemeAndHttpHost }}{{ path('legal_privacy') }}">Confidentialité</a>
            </p>
            <p style="font-size: 12px; margin-top: 15px;">
                Cet email confirme votre paiement pour Error Explorer.<br>
                Si vous avez des questions, n'hésitez pas à nous contacter.
            </p>
        </div>
    </div>
</body>
</html>