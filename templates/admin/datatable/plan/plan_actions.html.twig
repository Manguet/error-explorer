<div class="action-buttons">
    <a href="{{ path('admin_plans_edit', { id: row.id }) }}"
       class="btn btn-secondary btn-sm" title="Éditer">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
            <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path>
        </svg>
    </a>
    <button class="btn btn-sm {{ row.isActive ? 'btn-warning' : 'btn-success' }}"
            onclick="togglePlanStatus({{ row.id }})"
            title="{{ row.isActive ? 'Désactiver' : 'Activer' }}">
        {% if row.isActive %}
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="6" y="4" width="4" height="16"></rect>
                <rect x="14" y="4" width="4" height="16"></rect>
            </svg>
        {% else %}
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polygon points="5,3 19,12 5,21 5,3"></polygon>
            </svg>
        {% endif %}
    </button>
    {% set userCount = userCount|default(0) %}
    {% if userCount == 0 and not row.isPopular %}
        <button class="btn btn-sm btn-danger"
                onclick="deletePlan({{ row.id }}, '{{ row.name|e('js') }}')"
                title="Supprimer">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="3,6 5,6 21,6"></polyline>
                <path d="M19,6v14a2,2,0,0,1-2,2H7a2,2,0,0,1-2-2V6m3,0V4a2,2,0,0,1,2-2h4a2,2,0,0,1,2,2V6"></path>
            </svg>
        </button>
    {% endif %}
</div>
